	
	<ul class="pages">
		{% for page in pages %}
		<li>
			{% if page.page == page_this %} 
				{{ page.page }}
			{% else %}
				<a href="?page={{ page.page }}">{{ page.page }}</a>
			{% endif %}
		</li>
		{% endfor %}
	</ul>
	
